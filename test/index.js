/**
 * publish 
 */
exports.publish = {

  'Should set the post\'s published flag to true': function(test) {
    test.done();
  },

  'Should set the post\'s published flag to false': function(test) {
    test.done();
  },

};
